@model MendinePayroll.Models.EmployeeBonusModel
@{
    ViewBag.Title = "OverTimeDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*Add*@
<div class="modal fade modal-header-color zoom-anim-dialog" id="paygroup" tabindex="-1" role="dialog" aria-labelledby="">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <section class="">
                <header class="panel-heading bg-tertiary">
                    <button type="button" class="close pull-right" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h2 class="panel-title  text-white">Add New OverTime Details</h2>
                </header>
            </section>
            <div class="modal-body">
                <form>
                    <div class="row">
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Month <span class="required">*</span></label>
                                
                                   
                                    @Html.DropDownListFor(M=>M.Month,Model.MonthList,new { @class="form-control"})
                                    @*@Html.TextBoxFor(M => M.PayGroupName, new { @class = "form-control", @id = "txtPayGroupName" })*@
                                
                                <span id="msgMonth" class="ms-error" style="color:red;"></span>

                            </div>
                        </div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Year<span class="required">*</span></label>
                                @Html.DropDownListFor(M=>M.Year,Model.YearList,new { @class="form-control"})
                                <span id="msgDescriptionedit" style="color:red;"></span>
                            </div>
                        </div>
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">OverTime Hours <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @Html.TextBoxFor(M => M.OverTimeHours, new { @class = "form-control" })
                                    @*<input type="text" id="txtOvertimehours" class="form-control" />*@
                                </div>
                                <span id="msgMastercodeeedit" class="ms-error" style="color:red;"></span>
                            </div>
                        </div>

                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Production Bonus <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @*<input type="text" id="txtProductionbonus" class="form-control" />*@
                                    @Html.TextBoxFor(M => M.ProductionBonus, new { @class = "form-control" })
                                </div>
                                <span id="msgConcernHRPersonneledit" style="color:red;"></span>

                            </div>
                        </div>
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-12'>
                            <div class="form-group">
                                <label class="control-label">Target Achieved(%) <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @*<input type="text" id="txtTargetAchieved" class="form-control" />*@
                                    @Html.TextBoxFor(M => M.TargetAchieved, new { @class = "form-control" })
                                </div>
                                <span id="msgConcernHRPersonneledit" style="color:red;"></span>

                            </div>
                        </div>


                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" id="btnUpdateProduct" class="btn btn-tertiary" onclick="return SavePayGroupBonus();">
                    <i class="fa fa-plus-square space-right"></i> Save
                </button>
            </div>

            <div id="progress" style="display:none;">
                <div class="col-md-12 text-center">
                    <img src="~/Contents/images/ajax-loader.gif" />
                </div>
            </div>

        </div>
    </div>
</div>

@*Edit*@
<div class="modal fade modal-header-color zoom-anim-dialog" id="editpaygroup" tabindex="-1" role="dialog" aria-labelledby="">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <section class="">
                <header class="panel-heading bg-tertiary">
                    <button type="button" class="close pull-right" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h2 class="panel-title  text-white">Edit OverTime Details</h2>
                </header>
            </section>
            <div class="modal-body">
                <form>
                    <div class="row">
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Month <span class="required">*</span></label>
                                @Html.DropDownListFor(M => M.Month, Model.MonthList, new { @class = "form-control",@id="ddlMonthedit" })
                                @Html.HiddenFor(m=>m.ID)
                                <span id="msgMonthedit" class="ms-error" style="color:red;"></span>

                            </div>
                        </div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Year<span class="required">*</span></label>
                                @Html.DropDownListFor(M => M.Year, Model.YearList, new { @class = "form-control", @id = "ddlYearedit" })

                                <span id="msgDescriptionedit" style="color:red;"></span>
                            </div>
                        </div>
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">OverTime Hours <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @Html.TextBoxFor(M => M.OverTimeHours, new { @class = "form-control", @id = "txtOvertimehours" })
                                </div>
                                <span id="msgMastercodeeedit" class="ms-error" style="color:red;"></span>
                            </div>
                        </div>

                        <div class='col-sm-6'>
                            <div class="form-group">
                                <label class="control-label">Production Bonus <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @Html.TextBoxFor(M => M.ProductionBonus, new { @class = "form-control", @id = "txtProductionbonus" })
                                </div>
                                <span id="msgConcernHRPersonneledit" style="color:red;"></span>

                            </div>
                        </div>
                        <div class="col-md-12 clearfix-height20"></div>
                        <div class='col-sm-12'>
                            <div class="form-group">
                                <label class="control-label">Target Achieved(%) <span class="required">*</span></label>
                                <div class="input-group btn-group">
                                    <span class="input-group-addon">
                                        <i class="fa fa-cab"></i>
                                    </span>
                                    @Html.TextBoxFor(M => M.TargetAchieved, new { @class = "form-control", @id = "txtTargetAchieved" })
                                </div>
                                <span id="msgConcernHRPersonneledit" style="color:red;"></span>

                            </div>
                        </div>


                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" id="btnUpdateProduct" class="btn btn-tertiary" onclick="return UpdatePayGroupBonus();">
                    <i class="fa fa-plus-square space-right"></i> Update
                </button>
            </div>

            <div id="progress" style="display:none;">
                <div class="col-md-12 text-center">
                    <img src="~/Contents/images/ajax-loader.gif" />
                </div>
            </div>

        </div>
    </div>
</div>

@*Pay Group Bonus List*@
<section role="main" class="content-body">
    <input type="hidden" id="hdnPayGroupId" value="0" />
    <header class="page-header">
        <h2>OverTime Details</h2>
    </header>
    <!-- start: page -->
    <section class="panel">
        <header class="panel-heading">
            <div class="panel-actions">
                <button id="" onclick="BacktoPaygrouplist();" class="mb-sm mt-xs mr-xs  btn btn-sm btn-default text-sm text-weight-semibold"><i class="text-tertiary fa fa-arrow-circle-left  ml-lg"></i>Back</button>
                <button id="" onclick="AddPayGroupLoad();" data-toggle="modal" data-target="#paygroup" class="mb-sm mt-xs mr-xs btn btn-sm btn-tertiary text-sm text-weight-semibold">Add OverTime<i class="fa fa-plus ml-lg"></i></button>
            </div>
            <h2 class="panel-title">OverTime Details List</h2>
        </header>
        <div class="panel-body">

            <table class="table table-responsive mb-none" id="datatable-users">
                <thead>
                    <tr>
                        <th><i class="fa fa-credit-card text-muted space-right"></i>Month</th>
                        <th><i class="fa fa-credit-card text-muted space-right"></i>Year</th>
                        <th><i class="fa fa-credit-card text-muted space-right"></i>Overtime hours</th>
                        <th><i class="fa fa-credit-card text-muted space-right"></i>Production Bonus</th>
                        <th><i class="fa fa-credit-card text-muted space-right"></i>Target Achieved(%)</th>
                        <th class="center">MORE</th>

                    </tr>
                </thead>
                <tbody id="listVehicle">
                    @foreach(var items in Model.EmployeeBonusList)
                    {
                    <tr>
                        <td>@items.Month</td>
                        <td>@items.Year</td>
                        <td>@items.OverTimeHours</td>
                        <td>@items.ProductionBonus</td>
                        <td>@items.TargetAchieved</td>
                        <td>
                            <a href="#" onclick="return GetEmployeBonusdetails(@items.ID,@items.Empid);" data-toggle="modal" data-target="#detail">
                                <i class="text-tertiary fa fa-arrow-circle-o-right fa-lg"></i>
                            </a>
                        </td>
                    </tr>
                    }

                </tbody>
            </table>

        </div>

    </section>
    <!-- end: page -->
</section>

@*Pay Group Bonus Details *@
<div class="modal fade modal-header-color zoom-anim-dialog" id="detail" tabindex="-1" role="dialog" aria-labelledby="">
    <div class="modal-dialog" role="">
        <div class="modal-content">
            <section class="">
                <header class="panel-heading bg-quartenary">
                    <button type="button" class="close pull-right" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h2 class="panel-title  text-white">OverTime Details Details</h2>
                </header>
            </section>
            <div class="modal-body">
                <div class="row">

                    <div class="col-sm-5 col-xs-5"><a class="text-muted text-weight-bold">Month </a></div>
                    <div class="col-sm-7 col-xs-7"><a class="text-blue text-weight-semibold"><label id="lblMonth"></label></a></div>
                    <div class="col-sm-12 col-xs-12 pull-left"><hr class="solid short mt-xs"></hr></div>

                    <div class="col-sm-5 col-xs-5"><a class="text-muted text-weight-bold">Year </a></div>
                    <div class="col-sm-7 col-xs-7"><a class="text-blue text-weight-semibold"><label id="lblYear"></label></a></div>
                    <div class="col-sm-12 col-xs-12 pull-left"><hr class="solid short mt-xs"></hr></div>

                    <div class="col-sm-5 col-xs-5"><a class="text-muted text-weight-bold">OverTimeHours </a></div>
                    <div class="col-sm-7 col-xs-7"><a class="text-blue text-weight-semibold"><label id="lblOverTimeHours"></label></a></div>
                    <div class="col-sm-12 col-xs-12 pull-left"><hr class="solid short mt-xs"></hr></div>

                    <div class="col-sm-5 col-xs-5"><a class="text-muted text-weight-bold">Production Bonus </a></div>
                    <div class="col-sm-7 col-xs-7"><a class="text-blue text-weight-semibold"><label id="lblProductionBonus"></label></a></div>
                    <div class="col-sm-12 col-xs-12 pull-left"><hr class="solid short mt-xs"></hr></div>

                    <div class="col-sm-5 col-xs-5"><a class="text-muted text-weight-bold">Target Achieved(%) </a></div>
                    <div class="col-sm-7 col-xs-7"><a class="text-blue text-weight-semibold"><label id="lblTargetAchieved"></label></a></div>
                    <div class="col-sm-12 col-xs-12 pull-left"><hr class="solid short mt-xs"></hr></div>

                </div>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-quartenary" data-toggle="modal" data-target="#editpaygroup" data-dismiss="modal">
                    <i class="fa fa-pencil-square-o space-right"></i> Edit
                </button>
            </div>
        </div>
    </div>
</div>

<script>
    
    function SavePayGroupBonus() {
        
        var query = window.location.search.substring(1);
        var qsparam = new Array(10);
        var query = window.location.search.substring(1);
        var parms = query.split('&&');

        for (var i = 0; i < parms.length; i++) {
            var pos = parms[i].indexOf('=');
            if (pos > 0) {
                var key = parms[i].substring(0, pos);
                var val = parms[i].substring(pos + 1);
                qsparam[i] = val;
            }
        }
        var parms = query.indexOf('=');
        var empid = query.substring(parms + 1);
        var Empid = qsparam[0];
        var month = $("#Month option:selected").text();
        var year = $("#Year").val();
        var overtime = $("#OverTimeHours").val();
        var productionbonus = $("#ProductionBonus").val();
        var TargetAchieved = $("#TargetAchieved").val();
        var model = {
            sEmpid: Empid, Month: month, Year: year, OverTimeHours: overtime, ProductionBonus: productionbonus, TargetAchieved: TargetAchieved
        }
        var jsonData = JSON.stringify(model);
        var Id = 0;
        $.ajax({
            type: "POST",
            url: '/EmployeeList/SaveEmployeeBonus',

            data: function () {
                var data = new FormData();
                data.append("modelParameter", jsonData);
                
                return data;
            }(),

            contentType: false,
            processData: false,
            success: function (data) {
                
                if (data == "saved successfully") {

                    swal({
                        title: "Success!",
                        text: "OverTime has been Saved Successfully!",
                        type: "success"
                    }).then(function () {
                        window.location.href = "/EmployeeList/OverTimeDetails?cal=" + empid + "";
                        });

                }
                else if (data == "This month data of the year is alreay exist") {
                    $("#msgMonth").html("This month data of the year is alreay exist");
                }
            },
            error: function (er) {
                console.log(er);
            }
        });
    }
    function GetPayGroupBonusById(PayGroupId) {
        
        var jq = jQuery.noConflict();
        jq.ajax({
            type: "POST",
            url: '/PayGroup/GetPayGroupBonusById',
            data: { "ID": PayGroupId },
            datatype: JSON,
            success: function (response) {
                
                RecordLength = response.length;
                jq("#hdnPayGroupId").val(response[0].ID);
                jq("#lblMonth").text(response[0].Month);
                jq("#lblYear").text(response[0].Year);
                jq("#lblOverTimeHours").text(response[0].OverTimeHours);
                jq("#lblProductionBonus").text(response[0].ProductionBonus);
                jq("#lblTargetAchieved").text(response[0].TargetAchieved);
                jq("#ddlyearedit").val(response[0].Year);
                jq('#ddlmonthedit option').map(function () {
                    if ($(this).text() == response[0].Month) return this;
                }).attr('selected', 'selected');

                jq("#txtOvertimehours").val(response[0].OverTimeHours);
                jq("#txtProductionbonus").val(response[0].ProductionBonus);
                jq("#txtTargetAchieved").val(response[0].TargetAchieved);
            },
            error: function (er) {

                console.log(er);
            }
        });
    }

    function UpdatePayGroupBonus() {
        
        var qsparam = new Array(10);
        var query = window.location.search.substring(1);
        var parms = query.split('&&');

        for (var i = 0; i < parms.length; i++) {
            var pos = parms[i].indexOf('=');
            if (pos > 0) {
                var key = parms[i].substring(0, pos);
                var val = parms[i].substring(pos + 1);
                qsparam[i] = val;
            }
        }
        var parms = query.indexOf('=');
        var empid = query.substring(parms + 1);
        var Empid = qsparam[0];
        var month = $("#ddlMonthedit option:selected").text();
        var year = $("#ddlYearedit").val();
        var overtime = $("#txtOvertimehours").val();
        var productionbonus = $("#txtProductionbonus").val();
        var TargetAchieved = $("#txtTargetAchieved").val();
        var Id = $("#ID").val();
        var model = {
            ID:Id,sEmpid: Empid, Month: month, Year: year, OverTimeHours: overtime, ProductionBonus: productionbonus, TargetAchieved: TargetAchieved
        }
        var jsonData = JSON.stringify(model);
        var Id = 0;
        $.ajax({
            type: "POST",
            url: '/EmployeeList/SaveEmployeeBonus',

            data: function () {
                var data = new FormData();
                data.append("modelParameter", jsonData);

                return data;
            }(),

            contentType: false,
            processData: false,
            success: function (data) {
                
                if (data == "saved successfully") {

                    swal({
                        title: "Success!",
                        text: "OverTime has been Saved Successfully!",
                        type: "success"
                    }).then(function () {
                        window.location.href = "/EmployeeList/OverTimeDetails?cal=" + empid + "";
                    });

                }
                else if (data == "This month data of the year is alreay exist") {
                    $("#msgMonth").html("This month data of the year is alreay exist");
                }
            },
            error: function (er) {
                console.log(er);
            }
        });
    }

</script>

<script>
    function GetEmployeBonusdetails(ID,Empid) {
        var model = {
            ID:ID,Empid: Empid
        }
        var jsonData = JSON.stringify(model);
       
        $.ajax({
            type: "POST",
            url: '/EmployeeList/GetEmployeeBonus',

            data: function () {
                var data = new FormData();
                data.append("modelParameter", jsonData);

                return data;
            }(),

            contentType: false,
            processData: false,
            success: function (response) {
                
                $("#ID").val(response[0].ID);
                $("#ddlMonthedit option:contains(" + response[0].Month + ")").attr('selected', 'selected');
                $("#ddlYearedit").val(response[0].Year);
                $("#txtOvertimehours").val(response[0].OverTimeHours);

                $("#txtProductionbonus").val(response[0].ProductionBonus);
                $("#txtTargetAchieved").val(response[0].TargetAchieved);

                $("#lblYear").text(response[0].Year);
                $("#lblOverTimeHours").text(response[0].OverTimeHours);
                $("#lblMonth").text(response[0].Month);
                $("#lblProductionBonus").text(response[0].ProductionBonus);
                $("#lblTargetAchieved").text(response[0].TargetAchieved);
               
            },
            error: function (er) {
                console.log(er);
            }
        });
    }
</script>
<script>
    function BacktoPaygrouplist() {
        var qsparam = new Array(10);
        var query = window.location.search.substring(1);
        var parms = query.split('&&');

        for (var i = 0; i < parms.length; i++) {
            var pos = parms[i].indexOf('=');
            if (pos > 0) {
                var key = parms[i].substring(0, pos);
                var val = parms[i].substring(pos + 1);
                qsparam[i] = val;
            }
        }
        window.location.href = "/EmployeeList?SearchValue=" + qsparam[1] + "";
    }
</script>

